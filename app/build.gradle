buildscript {
    repositories {
        maven { url 'https://maven.fabric.io/public' }
    }

    dependencies {
        classpath 'io.fabric.tools:gradle:1.+'
    }
}
apply plugin: 'com.android.application'
apply plugin: 'io.fabric'

repositories {
    maven { url 'https://maven.fabric.io/public' }
}

apply plugin: 'kotlin-android'
apply plugin: 'com.google.gms.google-services'
apply plugin: 'kotlin-android-extensions'
apply plugin: 'com.github.triplet.play'

android {
    compileSdkVersion 28
    defaultConfig {
        applicationId "com.example.smsthemes"
        minSdkVersion 16
        targetSdkVersion 28
        versionCode 1
        versionName "1.0"
        multiDexEnabled true
        testInstrumentationRunner "android.support.test.runner.AndroidJUnitRunner"
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }

    lintOptions {
        checkReleaseBuilds false
    }

    playAccountConfigs {
        CrAccountConfig {
            serviceAccountEmail = '832332847631-compute@developer.gserviceaccount.com'
            pk12File = file('../p12keys/GooglePlayAndroidDeveloper-1aa366ac4467.p12')
        }
    }
    play {
        track = 'production' // or 'rollout' or 'beta' or 'alpha'
    }

    signingConfigs {
        costa_rica {
            Properties props = new Properties()
            props.load(new FileInputStream(file("../keystore/smscostarica.properties")))
            storeFile file(props['key.store'])
            storePassword props['key.store.password']
            keyAlias props['key.alias']
            keyPassword props['key.alias.password']
        }
    }

    dataBinding {
        enabled = true
    }
    productFlavors {
        cr_summer_sms_plus {
            applicationId "com.tj.summer.app"
            versionCode rootProject.ext.themeVersionCode
            versionName rootProject.ext.themeVersionName
            signingConfig signingConfigs.costa_rica
            playAccountConfig = playAccountConfigs.CrAccountConfig
            flavorDimensions "default"
        }
        cr_colorneon_sms_plus {
            applicationId "com.tj.colorneon.app"
            versionCode rootProject.ext.themeVersionCode
            versionName rootProject.ext.themeVersionName
            signingConfig signingConfigs.costa_rica
            playAccountConfig = playAccountConfigs.CrAccountConfig
            flavorDimensions "default"
        }
        cr_neon_colors_theme_sms_plus {
            applicationId "com.themejunky.neoncolors.theme.app"
            versionCode rootProject.ext.themeVersionCode
            versionName rootProject.ext.themeVersionName
            signingConfig signingConfigs.costa_rica
            playAccountConfig = playAccountConfigs.CrAccountConfig
            flavorDimensions "default"
        }
        cr_candy_sms_plus {
            applicationId "com.tj.candy.app"
            versionCode rootProject.ext.themeVersionCode
            versionName rootProject.ext.themeVersionName
            signingConfig signingConfigs.costa_rica
            playAccountConfig = playAccountConfigs.CrAccountConfig
            flavorDimensions "default"
        }
        cr_garden_sms_plus {
            applicationId "com.themejunky.garden.theme.app"
            versionCode rootProject.ext.themeVersionCode
            versionName rootProject.ext.themeVersionName
            signingConfig signingConfigs.costa_rica
            playAccountConfig = playAccountConfigs.CrAccountConfig
            flavorDimensions "default"
        }
        cr_tiger_sms_plus {
            applicationId "com.themejunky.pinkfashion.theme.app"
            versionCode rootProject.ext.themeVersionCode
            versionName rootProject.ext.themeVersionName
            signingConfig signingConfigs.costa_rica
            playAccountConfig = playAccountConfigs.CrAccountConfig
            flavorDimensions "default"
        }
        cr_pinkfashion_sms_plus {
            applicationId "com.themejunky.tiger.theme.app"
            versionCode rootProject.ext.themeVersionCode
            versionName rootProject.ext.themeVersionName
            signingConfig signingConfigs.costa_rica
            playAccountConfig = playAccountConfigs.CrAccountConfig
            flavorDimensions "default"
        }

    }
}

dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar'])
    implementation 'com.android.support:appcompat-v7:28.0.0'
    implementation('com.github.themejunky:SMSLayout2019:1.0.11') {

        exclude group: 'com.github.themejunky', module: 'gdx-backend-android'
        exclude group: 'com.github.themejunky', module: 'gdx'
        exclude group: 'com.github.themejunky', module: 'gdx-test'

        exclude group: 'com.github.themejunky', module: 'api'
        exclude group: 'com.github.themejunky', module: 'rippereffects-v.1.0.3'
    }
    implementation project(':api')
    implementation project(':gdx')
    implementation project(':gdx-backend-android')
    implementation project(':gdx-test')
    implementation project(':rippereffects-v.1.0.3')
    implementation 'com.crashlytics.sdk.android:crashlytics:2.9.9'
}
