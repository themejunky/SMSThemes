buildscript {
    repositories {
        maven { url 'https://maven.fabric.io/public' }
    }

    dependencies {
        classpath 'io.fabric.tools:gradle:1.+'
    }
}
apply plugin: 'com.android.application'
apply plugin: 'io.fabric'

repositories {
    maven { url 'https://maven.fabric.io/public' }
}

apply plugin: 'kotlin-android'
apply plugin: 'com.google.gms.google-services'
apply plugin: 'kotlin-android-extensions'
apply plugin: 'com.github.triplet.play'

android {
    compileSdkVersion 28
    defaultConfig {
        applicationId "com.example.smsthemes"
        minSdkVersion 16
        targetSdkVersion 28
        versionCode 1
        versionName "1.0"
        multiDexEnabled true
        testInstrumentationRunner "android.support.test.runner.AndroidJUnitRunner"
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }

    lintOptions {
        checkReleaseBuilds false
    }

    playAccountConfigs {
        AweAccountConfig {
            serviceAccountEmail = 'google-play-developer@api-6930642595758357419-976989.iam.gserviceaccount.com'
            pk12File = file('../p12keys/GooglePlayAndroidDeveloper-960c3811ddb3.p12')
        }
    }
    play {
        track = 'production' // or 'rollout' or 'beta' or 'alpha'
    }

    signingConfigs {
        themesforsms {
            Properties props = new Properties()
            props.load(new FileInputStream(file("../keystore/themesforsms.properties")))
            storeFile file(props['key.store'])
            storePassword props['key.store.password']
            keyAlias props['key.alias']
            keyPassword props['key.alias.password']
        }
    }

    dataBinding {
        enabled = true
    }
    productFlavors {
        /*aws_pinkhd_sms_plus {
            applicationId "com.theme.sms.pinkhd.app"
            versionCode rootProject.ext.themeVersionCode
            versionName rootProject.ext.themeVersionName
            signingConfig signingConfigs.themesforsms
            playAccountConfig = playAccountConfigs.AweAccountConfig
            flavorDimensions "default"
        }
        aws_pink_sms_plus {
            applicationId "com.aws.sms.pink.app"
            versionCode rootProject.ext.themeVersionCode
            versionName rootProject.ext.themeVersionName
            signingConfig signingConfigs.themesforsms
            playAccountConfig = playAccountConfigs.AweAccountConfig
            flavorDimensions "default"
        }
        aws_whatsapp_sms_plus {
            applicationId "com.aws.sms.whatsapp.app"
            versionCode rootProject.ext.themeVersionCode
            versionName rootProject.ext.themeVersionName
            signingConfig signingConfigs.themesforsms
            playAccountConfig = playAccountConfigs.AweAccountConfig
            flavorDimensions "default"
        }
        aws_messenger_sms_plus {
            applicationId "com.aws.sms.messenger.app"
            versionCode rootProject.ext.themeVersionCode
            versionName rootProject.ext.themeVersionName
            signingConfig signingConfigs.themesforsms
            playAccountConfig = playAccountConfigs.AweAccountConfig
            flavorDimensions "default"
        }
        aws_pinkapple_sms_plus {
            applicationId "com.aws.sms.pinkapple.app"
            versionCode rootProject.ext.themeVersionCode
            versionName rootProject.ext.themeVersionName
            signingConfig signingConfigs.themesforsms
            playAccountConfig = playAccountConfigs.AweAccountConfig
            flavorDimensions "default"
        }
        aws_galaxynote8_sms_plus {
            applicationId "com.aws.sms.galaxynote8.app"
            versionCode rootProject.ext.themeVersionCode
            versionName rootProject.ext.themeVersionName
            signingConfig signingConfigs.themesforsms
            playAccountConfig = playAccountConfigs.AweAccountConfig
            flavorDimensions "default"
        }
        aws_applepinkphone8_sms_plus {
            applicationId "com.aws.sms.applepinkphone8.app"
            versionCode rootProject.ext.themeVersionCode
            versionName rootProject.ext.themeVersionName
            signingConfig signingConfigs.themesforsms
            playAccountConfig = playAccountConfigs.AweAccountConfig
            flavorDimensions "default"
        }
        aws_pinkdiamondparis_sms_plus {
            applicationId "com.aws.sms.pinkdiamondparis.app"
            versionCode rootProject.ext.themeVersionCode
            versionName rootProject.ext.themeVersionName
            signingConfig signingConfigs.themesforsms
            playAccountConfig = playAccountConfigs.AweAccountConfig
            flavorDimensions "default"
        }
        aws_pinkphone8_sms_plus {
            applicationId "com.aws.sms.pinkphone8.app"
            versionCode rootProject.ext.themeVersionCode
            versionName rootProject.ext.themeVersionName
            signingConfig signingConfigs.themesforsms
            playAccountConfig = playAccountConfigs.AweAccountConfig
            flavorDimensions "default"
        }
        aws_pinkphonex_sms_plus {
            applicationId "com.aws.sms.pinkphonex.app"
            versionCode rootProject.ext.themeVersionCode
            versionName rootProject.ext.themeVersionName
            signingConfig signingConfigs.themesforsms
            playAccountConfig = playAccountConfigs.AweAccountConfig
            flavorDimensions "default"
        }
        aws_snapchat_sms_plus {
            applicationId "com.aws.sms.snapchat.app"
            versionCode rootProject.ext.themeVersionCode
            versionName rootProject.ext.themeVersionName
            signingConfig signingConfigs.themesforsms
            playAccountConfig = playAccountConfigs.AweAccountConfig
            flavorDimensions "default"
        }
        aws_telegram_sms_plus {
            applicationId "com.aws.sms.telegram.app"
            versionCode rootProject.ext.themeVersionCode
            versionName rootProject.ext.themeVersionName
            signingConfig signingConfigs.themesforsms
            playAccountConfig = playAccountConfigs.AweAccountConfig
            flavorDimensions "default"
        }
        aws_pixel_sms_plus {
            applicationId "com.aws.sms.pixel.app"
            versionCode rootProject.ext.themeVersionCode
            versionName rootProject.ext.themeVersionName
            signingConfig signingConfigs.themesforsms
            playAccountConfig = playAccountConfigs.AweAccountConfig
            flavorDimensions "default"
        }
*/       aws_halloween_sms_plus {
            applicationId "com.aws.sms.halloween.app"
            versionCode rootProject.ext.themeVersionCode
            versionName rootProject.ext.themeVersionName
            signingConfig signingConfigs.themesforsms
            playAccountConfig = playAccountConfigs.AweAccountConfig
            flavorDimensions "default"
        }
    }
}

dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar'])
    implementation 'com.android.support:appcompat-v7:28.0.0'
    implementation('com.github.themejunky:SMSLayout2019:1.0.11') {

        exclude group: 'com.github.themejunky', module: 'gdx-backend-android'
        exclude group: 'com.github.themejunky', module: 'gdx'
        exclude group: 'com.github.themejunky', module: 'gdx-test'

        exclude group: 'com.github.themejunky', module: 'api'
        exclude group: 'com.github.themejunky', module: 'rippereffects-v.1.0.3'
    }
    implementation project(':api')
    implementation project(':gdx')
    implementation project(':gdx-backend-android')
    implementation project(':gdx-test')
    implementation project(':rippereffects-v.1.0.3')
    implementation 'com.crashlytics.sdk.android:crashlytics:2.9.9'
}
